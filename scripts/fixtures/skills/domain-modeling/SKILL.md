# Domain modeling fixture skill

This checked-in fixture stands in for a selected runtime skill during the
portable bootstrap verification. It is intentionally a real, non-empty
`SKILL.md` file so the verifier exercises the complete read path.
