export default function Home() {
  return (
    <main className="mx-auto max-w-4xl px-6 py-16">
      <h1 className="text-4xl font-semibold tracking-tight">
        HomeDesign Clone
      </h1>
      <p className="mt-4 text-lg opacity-80">
        Foundation harness for the HomeDesign clone (spec #13): Next.js + OpenNext
        on Cloudflare Workers, workers runtime test harness, fake AI provider seam,
        Playwright visual baselines, and fixture helpers.
      </p>
      <p className="mt-8 font-mono text-sm opacity-60">
        Ticket 01 — workers + browser/visual test harness. UI arrives in later tickets.
      </p>
    </main>
  );
}
