// Application shell components (ticket 02) — header, footer, and the root
// layout every route mounts into. Matches DESIGN.md section 2 (Layout & Shell)
// and section 4 (Header / Nav, Footer): paper bg, ink text, pill buttons,
// sticky header with backdrop blur, footer with brand + 3 nav columns.

export { Header } from "./header";
export { Footer } from "./footer";
